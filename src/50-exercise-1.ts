/**
 * Exercise 1: Syntax & Basic Types
 * run `npm run try 1` to check your work
 */


// (++) Add type 'number' to someNumber variable
let someNumber;

// (++) Add type 'string' to someString variable
let someString;

// (++) Add type 'ESCAPE' to someLiteral variable
let someLiteral;



// (++) Fix the values to reflect the type
someNumber = '100';

someString = 100.toString();

someLiteral = 'OK?';


console.log('someNumber ->', someNumber);
console.log('someString ->', someString);
console.log('someLiteral ->', someLiteral);
